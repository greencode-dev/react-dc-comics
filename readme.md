# DC COMICS - PARTE 1

## Obiettivo: Lavorare con i componenti su React

**Repo:** `react-dc-comics`

---

### DESCRIZIONE:

Create un nuovo progetto React e definite i componenti necessari per strutturare il layout come da screenshot.

Per oggi, la priorità è la struttura. Una volta solida, si potranno sostituire i segnaposto e aggiungere il CSS.

---

### BONUS:

- Creare un componente aggiuntivo per gestire la fascia azzurra con le icone.
- Provare a spostare il CSS in moduli per componente.

---

### NOTE:

- **Ambiente di Sviluppo:** Per visualizzare il progetto, è necessario lanciare il comando `npm run dev` e aprire il browser all'indirizzo `localhost` indicato.
- ⚠️ **Importante:** Non è possibile usare l'estensione `live-server` né aprire il file `index.html` direttamente nel browser. Il server di sviluppo di React è necessario per far funzionare l'applicazione.
- **Sviluppo in Fasi:** Questo è un esercizio in più fasi. La sezione "main" per ora sarà un'area nera con un segnaposto "content qui", che verrà sviluppata successivamente.
- **Materiali:** Trovate i materiali necessari in allegato e il live coding di riferimento.

### 🖼️ Esempio del risultato

Ecco uno screenshot di come potrebbe apparire il risultato finale:

![Screenshot del progetto](./dc-comics-screenshot.png)
